<?php

/**
 * Filament 配置文件
 * 
 * 由 Webman Filament 配置生成器自动生成
 * 生成时间: {{TIMESTAMP}}
 */

return [

    /*
    |--------------------------------------------------------------------------
    | 应用信息
    |--------------------------------------------------------------------------
    */

    'app_name' => '{{APP_NAME}}',
    'url' => '{{APP_URL}}',
    'timezone' => '{{APP_TIMEZONE}}',
    'locale' => '{{APP_LOCALE}}',

    /*
    |--------------------------------------------------------------------------
    | 品牌配置
    |--------------------------------------------------------------------------
    */

    'brand' => '{{FILAMENT_BRAND}}',

    /*
    |--------------------------------------------------------------------------
    | 数据库连接
    |--------------------------------------------------------------------------
    */

    'database_connection' => '{{DB_CONNECTION}}',

    /*
    |--------------------------------------------------------------------------
    | 缓存配置
    |--------------------------------------------------------------------------
    */

    'cache_prefix' => '{{CACHE_PREFIX}}',

    /*
    |--------------------------------------------------------------------------
    | 功能开关
    |--------------------------------------------------------------------------
    */

    'features' => [
        'dark_mode' => {{DARK_MODE}},
        'notifications' => {{NOTIFICATIONS}},
        'widgets' => {{WIDGETS}},
    ],

    /*
    |--------------------------------------------------------------------------
    | 页面配置
    |--------------------------------------------------------------------------
    */

    'pages' => [
        'dashboard' => [
            'title' => '仪表盘',
            'icon' => 'heroicon-o-home',
            'sort' => 1,
        ],
        'users' => [
            'title' => '用户管理',
            'icon' => 'heroicon-o-users',
            'sort' => 2,
        ],
        'settings' => [
            'title' => '系统设置',
            'icon' => 'heroicon-o-cog-6-tooth',
            'sort' => 999,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 资源配置
    |--------------------------------------------------------------------------
    */

    'resources' => [
        'columns' => [
            'default' => 1,
            'sm' => 1,
            'md' => 2,
            'lg' => 3,
            'xl' => 4,
            '2xl' => 5,
        ],
        'forms' => [
            'default' => 1,
            'sm' => 1,
            'md' => 2,
            'lg' => 2,
            'xl' => 3,
            '2xl' => 3,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 通知配置
    |--------------------------------------------------------------------------
    */

    'notifications' => [
        'enabled' => {{NOTIFICATIONS}},
        'position' => 'top-right',
        'duration' => 5000,
    ],

    /*
    |--------------------------------------------------------------------------
    | 小组件配置
    |--------------------------------------------------------------------------
    */

    'widgets' => [
        'enabled' => {{WIDGETS}},
        'dashboard' => [
            'default' => [
                \Filament\Widgets\StatsOverviewWidget::class,
            ],
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 主题配置
    |--------------------------------------------------------------------------
    */

    'theme' => [
        'dark_mode' => {{DARK_MODE}},
        'primary_color' => '#3b82f6',
        'gray_color' => '#6b7280',
        'layout' => [
            'max_content_width' => null,
            'sidebar_width' => '16rem',
            'topbar_height' => '4rem',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 认证配置
    |--------------------------------------------------------------------------
    */

    'auth' => [
        'guard' => 'web',
        'middleware' => [
            'auth' => \WebmanFilament\Middleware\AuthMiddleware::class,
        ],
        'login_page' => \Filament\Http\Livewire\Auth\Login::class,
        'profile_page' => \Filament\Pages\SettingsPage::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | 文件上传配置
    |--------------------------------------------------------------------------
    */

    'files' => [
        'disk' => 'public',
        'directory' => 'filament',
        'max_size' => 1024 * 1024 * 2, // 2MB
        'allowed_types' => ['jpg', 'jpeg', 'png', 'gif', 'pdf', 'doc', 'docx'],
    ],

    /*
    |--------------------------------------------------------------------------
    | 搜索配置
    |--------------------------------------------------------------------------
    */

    'search' => [
        'enabled' => true,
        'min_characters' => 2,
        'max_results' => 50,
    ],

    /*
    |--------------------------------------------------------------------------
    | 导出配置
    |--------------------------------------------------------------------------
    */

    'exports' => [
        'enabled' => true,
        'formats' => ['csv', 'xlsx', 'pdf'],
        'chunk_size' => 1000,
    ],

    /*
    |--------------------------------------------------------------------------
    | 调试配置
    |--------------------------------------------------------------------------
    */

    'debug' => env('FILAMENT_DEBUG', false),

    /*
    |--------------------------------------------------------------------------
    | 性能配置
    |--------------------------------------------------------------------------
    */

    'performance' => [
        'cache_enabled' => env('FILAMENT_CACHE_ENABLED', true),
        'preload_assets' => env('FILAMENT_PRELOAD_ASSETS', false),
        'minify_assets' => env('FILAMENT_MINIFY_ASSETS', true),
    ],

    /*
    |--------------------------------------------------------------------------
    | 开发者工具
    |--------------------------------------------------------------------------
    */

    'dev_tools' => env('FILAMENT_DEV_TOOLS', false),

    /*
    |--------------------------------------------------------------------------
    | 面板配置
    |--------------------------------------------------------------------------
    */

    'panels' => [
        'admin' => [
            'path' => '/admin',
            'auth' => [
                'guard' => 'web',
                'pages' => [
                    'login' => \Filament\Http\Livewire\Auth\Login::class,
                ],
            ],
            'pages' => [
                'dashboard' => \Filament\Pages\Dashboard::class,
            ],
            'widgets' => [
                'account' => \Filament\Widgets\AccountWidget::class,
                'filament_info' => \Filament\Widgets\FilamentInfoWidget::class,
            ],
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 插件配置
    |--------------------------------------------------------------------------
    */

    'plugins' => [
        // 在这里添加 Filament 插件配置
    ],

    /*
    |--------------------------------------------------------------------------
    | 自定义配置
    |--------------------------------------------------------------------------
    */

    'custom' => [
        // 在这里添加自定义配置项
    ],

    /*
    |--------------------------------------------------------------------------
    | 中间件配置
    |--------------------------------------------------------------------------
    */

    'middleware' => [
        'global' => [
            \WebmanFilament\Middleware\FilamentMiddleware::class,
        ],
        'auth' => [
            \WebmanFilament\Middleware\AuthMiddleware::class,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 路由配置
    |--------------------------------------------------------------------------
    */

    'routes' => [
        'prefix' => '/admin',
        'middleware' => [
            'web',
            'auth',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 静态资源配置
    |--------------------------------------------------------------------------
    */

    'assets' => [
        'js' => [
            'filament.js',
            'alpine.js',
        ],
        'css' => [
            'filament.css',
            'tailwindcss.css',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 事件配置
    |--------------------------------------------------------------------------
    */

    'events' => [
        'listeners' => [
            // 在这里添加事件监听器
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 监听器配置
    |--------------------------------------------------------------------------
    */

    'listeners' => [
        // 在这里添加自定义监听器
    ],

];